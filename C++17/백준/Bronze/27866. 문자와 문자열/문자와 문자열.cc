#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int A;
    string s;
    cin>>s;
    cin>>A;
    cout<<s[A-1]<<"\n";     
}